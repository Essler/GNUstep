#
include $(GNUSTEP_MAKEFILES)/common.make
# Make a simple obj-c program
TOOL_NAME = tutorial
tutorial_OBJC_FILES = main.m
# Header files
#tutorial_HEADER_FILES = xxx.h
# Compilation flags
ADDITIONAL_CPPFLAGS = -Wall -Wno-import
# Rules for creating a command line tool
include $(GNUSTEP_MAKEFILES)/tool.make